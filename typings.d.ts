// Define the EventData structure as needed
type EventData = {
  id: number;
  title: string;
  desc?: string;
  imgUrl?: string;
  isChecked: boolean;
};

type Icon = {
  name: string;
  icon: React.JSX.Element;
};
